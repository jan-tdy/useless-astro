import { defineConfig } from 'astro/config';

// Project GitHub Pages site: https://jan-tdy.github.io/useless-astro/
export default defineConfig({
  site: 'https://jan-tdy.github.io',
  base: '/useless-astro',
  trailingSlash: 'never',
});
